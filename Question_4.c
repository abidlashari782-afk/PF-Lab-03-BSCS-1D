#include<stdio.h>

int main(){
    //calculte the area of traingle
int base, height, area;
printf("Enter the value of Height: ");
scanf("%d", &height);
printf("Enter The value of Base: ");
scanf("%d",&base);
 area = 0.5*base*height;
 printf("Area of traingle is %d", area);
}
